import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color primary = Color(0xFF48CFCB);
  static const Color primaryDark = Color(0xFF229799);
  static const Color text = Color(0xFF424242);
  static const Color grey = Color(0xFF9E9E9E);
  static const Color white = Colors.white;

  // Event card colors
  static const Color eventIconBg = Color(0xFFF5C6EC);

  // Shadow colors
  // static Color shadowColor = grey.withOpacity(0.08);
}
